require 'spec_helper'

describe PlumbusHttp do
  it 'has a version number' do
    expect(PlumbusHttp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
